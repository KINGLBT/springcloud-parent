server:
  port: 8081

spring:
  security:
    user:
      name: zytech
      password: zytech2018

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/